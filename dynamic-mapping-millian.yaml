batcmd: 'batsim -p ${BATSIM_DIR}/platforms/small_platform.xml
                -w ${BATSIM_DIR}/workloads/test_smpi_compute_only_multijobs.json
                -e /tmp/dynamic-mapping-out/out'
failure-timeout: 0
output-dir: /tmp/dynamic-mapping-out
ready-timeout: 3
schedcmd: "batsched -v filler --variant_options '{\"custom_mapping\": true}'"
simulation-timeout: 30
success-timeout: 3
